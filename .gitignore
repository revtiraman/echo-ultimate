# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
venv/
.venv/
# NOTE: env/ is our source code package — do NOT ignore it
# ENV/ excluded (case-insensitive macOS would match env/ so we skip this)

# Jupyter
.ipynb_checkpoints/

# Data (downloaded datasets — too large for git)
data/*.json
data/*.jsonl
data/*.parquet
data/*.arrow
data/*.csv

# Trained model weights (too large for git)
results/echo_trained/

# Training logs (regenerated)
results/training_log.csv
results/baseline_log.json

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Secrets
.env
*.key
